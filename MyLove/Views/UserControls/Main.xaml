<UserControl x:Class="MyLove.Views.UserControls.Main"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyLove.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             FontFamily="{StaticResource Philosopher}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="5*" />
        </Grid.RowDefinitions>
        <TextBlock
            FontSize="64"
            HorizontalAlignment="Center"
            Text="Highway to history" />
        <TextBlock
            Grid.Row="1"
            HorizontalAlignment="Center"
            FontSize="22"
            Text="{DynamicResource TheBest}" />
        <Image
            Grid.Row="1"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Source="D:\University\coursach\MyLove\MyLove\DesignElements\Pictures\142.png" />
    </Grid>
</UserControl>
